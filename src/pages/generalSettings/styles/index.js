import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  container: {
    paddingTop: 100,
    margin: "0 auto",
    display: "flex",
    flexDirection: "column",
    height: "100%",
    width: 500,
  },
  buttonContainer: {
    padding: "10px 15px",
  },
}));
